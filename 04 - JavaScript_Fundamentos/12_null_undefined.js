//Variável não inicializada
let valor;
console.log(valor);//undefined = nada foi atribuido para a variável

valor = null;//Agora atribuimos o "vazio" para a variável
console.log(valor);

const produto = {};
console.log(produto.preco);//preco ainda não foi definido -> undefined



