const [a, b, c, d] = [1, 2, 3, 4];

const soma = a + b + c + d;
const sub = a - b - c - d;
const mult = a * b * c * d; 
const divisao = a / b;
const modulo = a % 2;

console.log(soma, sub, mult, divisao, modulo);